specVersion: "v1"
serviceId: "benchlot"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "benchlot-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
